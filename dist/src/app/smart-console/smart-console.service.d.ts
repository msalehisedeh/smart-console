import { EventEmitter } from '@angular/core';
export interface SmartOptions {
    emitOutput?: boolean;
    logAfterEmit?: boolean;
    logDisabled?: boolean;
    infoDisabled?: boolean;
    warnDisabled?: boolean;
    errorDisabled?: boolean;
    tableDisabled?: boolean;
    traceDisabled?: boolean;
    exceptionDisabled?: boolean;
    debugDisabled?: boolean;
    assertDisabled?: boolean;
    downGrade?: boolean;
    upgrade?: boolean;
    upscale?: boolean;
    throttleOn?: number;
    blockCaller?: any[];
    suppress?: any[];
    filter?: any[];
}
export declare class SmartConsoleService {
    private options;
    private defaultLog;
    private defaultInfo;
    private defaultWarn;
    private defaultError;
    private defaultTable;
    private defaultTrace;
    private defaultAssert;
    private defaultException;
    private defaultDebug;
    private output;
    private watchList;
    private _argsToString;
    private _suppressed;
    private _filtered;
    private _getStack;
    private _blocked;
    private _throttle;
    private _reportWatch;
    private _upscale;
    private _info;
    private _log;
    private _warn;
    private _error;
    private _table;
    private _trace;
    private _exception;
    private _debug;
    private _assert;
    makeSmartLogs(instructions: SmartOptions): void;
    redirectedOutput(): EventEmitter<any>;
    addWatch(key: any): any;
    removeWatch(key: any): void;
    clearWatchList(list: any[]): void;
    markupTrace(args: any): any;
}
